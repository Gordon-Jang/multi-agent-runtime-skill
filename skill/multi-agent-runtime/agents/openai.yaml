interface:
  display_name: "Multi-Agent Runtime"
  short_description: "Diagnose, run, and manage the local multi-agent runtime"
  default_prompt: "Use $multi-agent-runtime to diagnose the local multi-agent runtime, run a team, or update agent definitions."
